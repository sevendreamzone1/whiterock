# Empty uses the same origin. In local Vite dev, /api is proxied to http://localhost:3000.
VITE_API_BASE_URL=

# To test against the deployed API from local React dev:
# VITE_API_BASE_URL=https://whiterock-ashy.vercel.app
